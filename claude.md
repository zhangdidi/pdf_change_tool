# AI 开发专属规范 - 前端文件转换工具项目
## 项目定位
纯前端离线免费文件转换网页工具，支持 PDF、Word、Excel、PNG、JPG、WebP 相互转换，所有功能浏览器本地运行，无后端、无付费接口、无数据上传。

## 技术栈严格约束
1. 框架：Vue3 + TypeScript + Vite，全程 <script setup lang="ts">
2. 样式：TailwindCSS，不写自定义css，简洁极简UI
3. 状态管理：极简需求不引入Pinia，组件内部ref/reactive即可
4. 工具库：仅使用指定开源免费库，禁止引入付费/后端依赖
   - pdfjs-dist、pdf-lib（PDF处理）
   - xlsx（Excel读写转换）
   - office-converter-wasm（Word/Excel转PDF/图片）
   - browser-image-compression（图片格式互转、压缩）
   - file-saver、js-zip（文件下载、批量打包）
5. 代码规范：TS严格类型校验、无any类型、函数拆分、注释清晰、错误捕获完善

## 功能需求（必须全部实现）
### 核心转换功能
1. 图片互转：PNG ↔ JPG ↔ WebP，支持压缩、质量调节
2. 图片转PDF：多张图片合并为单个PDF
3. PDF转图片：PDF逐页转高清PNG/JPG，支持单页/全部导出
4. Word(docx) ↔ PDF 双向转换
5. Excel(xlsx) ↔ PDF 双向转换
6. Word/Excel 转图片格式

### 交互需求
1. 支持点击上传 + 拖拽上传文件
2. 限制文件大小、格式校验，错误提示友好
3. 转换中loading加载状态、进度条显示
4. 转换成功一键下载，多文件自动打包ZIP
5. 转换失败清晰报错（格式错误/文件损坏/大小超限）
6. 响应式布局，适配电脑/手机端

## 开发硬性规则
1. 所有文件转换逻辑纯前端离线执行，禁止请求任何第三方转换接口
2. 所有TS类型严格定义，杜绝隐式any
3. 代码模块化：拆分上传模块、转换模块、下载模块、校验模块
4. 全局异常捕获，避免页面白屏、卡死
5. UI极简干净、无广告、无冗余功能，专注转换核心
6. 支持大文件分片处理，避免浏览器崩溃

## 迭代规则
1. 优化优先：性能优化、兼容性优化、容错优化
2. 新增功能必须兼容现有逻辑
3. 修改代码保留原有结构，不重构全局
4. 每次开发补充详细注释，方便后续AI迭代

## 输出要求
1. 输出完整可直接运行的代码，包含文件路径
2. 每段代码附带简要功能说明
3. 报错优先给出修复后完整代码，不输出零散片段
4. 新增功能同步更新类型定义与工具方法